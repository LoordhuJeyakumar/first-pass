# First Pass — Operator Console dependencies.
# Installed once into the shared .venv (see frontend/README.md).
# Verified to resolve on Python 3.14.6 on 2026-08-15.

fastapi==0.141.1
uvicorn[standard]==0.52.1
jinja2==3.1.6
