# First Pass — runtime and test dependencies.
# Google AI SDK packages (google-adk, google-genai, etc.) will be added here
# as each agent module is implemented. Do not list packages before they are imported.

requests==2.33.1
pytest==9.1.1
